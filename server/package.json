{
  "name": "kakeibo-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "レシート解析API（Claude API をサーバー側からのみ呼び出す）",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
